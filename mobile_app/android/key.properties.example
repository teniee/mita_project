# Android Keystore Properties
# IMPORTANT: This file contains sensitive information. DO NOT commit to version control!
# Copy this file to key.properties and fill in your actual values.

# Path to your keystore file (relative to android/ directory)
storeFile=upload-keystore.jks

# Keystore password
storePassword=YOUR_KEYSTORE_PASSWORD

# Key alias
keyAlias=upload

# Key password
keyPassword=YOUR_KEY_PASSWORD

# HOW TO CREATE A KEYSTORE:
# Run this command in the android/app directory:
# keytool -genkey -v -keystore upload-keystore.jks -keyalg RSA -keysize 2048 -validity 10000 -alias upload

# Then add android/key.properties to .gitignore to keep it secure!
